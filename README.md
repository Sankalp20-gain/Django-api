Django Task Api
- simple backend , which can fetch and create Tasks using JSON requests
- it handles GET and POST request from user(tested with POSTMAN)
- Docker Configuration used so that it can smoothly run on any system
- Endpoint: /djangoApi/tasks/

TECH-STACK = Python,Docker,Django,SQlite
